# Variáveis para testes de API (Newman)
# Uso: cp .env.test.example .env.test

# Base dos webhooks n8n (instância Bold)
API_BASE_URL=https://dev.boldsolution.com.br/webhook

# Agente de teste (docs/installation.md — só dev)
TEST_AGENT_EMAIL=agente@bold.com
TEST_AGENT_PASSWORD=Bold@2026
